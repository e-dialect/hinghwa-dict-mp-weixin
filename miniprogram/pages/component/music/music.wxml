<cu-custom bgColor="bg-white" isBack="{{true}}">
  <view slot="content" class="text-black">语记·音乐</view>
</cu-custom>
<button class="cu-btn icon lg bg-blue shadow write" bindtap="uploadMusic">
  <text class="cuIcon-musicfill"></text>
</button>
<view wx:for="{{music}}">
  <audio poster="{{item.music.cover}}" name="{{item.music.title}}" author="{{item.music.artist}}"
    src="{{item.music.source}}" id="myAudio" controls loop></audio>
</view>
